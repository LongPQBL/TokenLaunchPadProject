{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**", ".next/**"] },
    "typecheck": { "dependsOn": ["^build"], "env": ["DATABASE_URL"] },
    "test": { "dependsOn": ["^build"], "env": ["DATABASE_URL"] }
  }
}
