export type { Candle, Comment, CommentPage, Health, Holder, Report, TokenDetail, TokenListItem, TokenPage, Trade, TradePage } from "./schemas";

export type TokenSort = "new" | "volume" | "progress";
