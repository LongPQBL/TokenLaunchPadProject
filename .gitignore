# Dependencies and build output
node_modules/
.next/
.next-demo/
out/
build/
coverage/

# Secrets and local config: never commit
.env
.env.*
!.env.example
id.json
*.pem
*.keypair.json

# OS / editor
.DS_Store

# Proprietary TradingView Charting Library (needs its own licence, do not publish)
TokenLaunchpad-fe/legacy-solana/public/libraries/

# Ponder / example build output
.ponder/
ponder-env.d.ts
package-lock.json

# Ponder's generated GraphQL schema (the example's is tracked; the app's is regenerated by codegen)
TokenLaunchpad-be/apps/indexer/generated/

# A local-chain deployment differs on every Anvil run; scripts/local-chain.sh regenerates it
TokenLaunchpad-be/packages/deployments/local.json

# Playwright output
test-results/
playwright-report/

# Next.js generates these
next-env.d.ts
*.tsbuildinfo

# Monorepo tooling
.turbo/
dist/
*.log

# Project documents are written but never committed
TokenLaunchpad-be/docs/

# Scratch state of the plan executor (ledger, briefs, review packages)
.superpowers/
